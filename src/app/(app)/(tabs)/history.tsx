import { Text } from "react-native";

import Screen from "@/components/ui/screen";

const History = () => {
  return (
    <Screen>
      <Text>History</Text>
    </Screen>
  );
};

export default History;

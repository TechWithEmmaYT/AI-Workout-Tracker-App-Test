import { Text } from "react-native";

import Screen from "@/components/ui/screen";

const Profile = () => {
  return (
    <Screen>
      <Text>Profile</Text>
    </Screen>
  );
};

export default Profile;
